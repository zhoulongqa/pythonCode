将对象的数据与操作数据的方法相结合，通过方法将对象的数据与实现细节保护
起来，就称为封装
外界只能通过对象的方法访问对象，因此封装同时也实现了对象的数据隐藏。通过
封装和数据隐藏机制，将一个对象相关的变量和方法封装为一个独立的软件体，单独
进行实现和维护，并使对象能够在系统内方便地进行传递，另外也保证了对象数据的
一致性并使程序易于维护，封装可以让调用者不用关心对象是如何构建的而直接进行
使用
在python中，所有的特性都是公开可用的，但是程序员应该在直接访问对象数据与操作
数据时谨慎行事，因为他们可能无意中使得这些特性在某些方面不一致
